#include<stdio.h>
int main(){
	int x=0,n,i,j;
	char ch[10];
	scanf("%d",&n);
	for(i=0;i<n;i++){
		scanf("%s",&ch);
		for(j=0;j<3;j++){
			if(ch[j]=='+'){
			x++;
			break;
		}
		else if(ch[j]=='-'){
			x--;
			break;
		}
		}
		
	}
	printf("%d\n",x);
	return 0;
}
