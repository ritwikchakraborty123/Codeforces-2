#include<stdio.h>
#include<string.h>
int main(){
	char str[1000];
	int i=0;
	scanf("%s",&str);
	if(str[i] >= 'A' && str[i] <= 'Z'){
		printf("%s\n",str);
	}
	else if(str[i] >='a' && str[i] <= 'z'){
		str[i]=str[i]-32;
		printf("%s\n",str);
	}
	return 0;
}
